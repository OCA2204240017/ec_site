<!DOCTYPE html>
<html>
  <head>
    <title>Admin</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>
  <body>
    <header>
      <nav>
        <%= link_to '書籍管理', admin_books_path %> |
        <%= link_to 'ユーザー', admin_users_path %> |
        <%= link_to 'ログアウト', admin_session_path, method: :delete %>
      </nav>
    </header>
    <main>
      <% flash.each do |k, v| %>
        <p class="flash <%= k %>"><%= v %></p>
      <% end %>
      <%= yield %>
    </main>
  </body>
 </html>
